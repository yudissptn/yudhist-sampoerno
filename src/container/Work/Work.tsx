import { ParallaxLayer } from "@react-spring/parallax";

const Work = () => {
  return (
    <ParallaxLayer offset={1.5} speed={2} className="work_container">
      Work
    </ParallaxLayer>
  );
};

export default Work;
